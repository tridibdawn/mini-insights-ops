# JWT Secret Key (Change this in production!)
JWT_SECRET=your-secret-key-change-in-production

# Node Environment
NODE_ENV=development

# Mapbox Access Token (Optional - OpenStreetMap used as fallback)
# IMPORTANT: Use a PUBLIC token (starts with pk.) NOT a secret token (sk.)
# To get a public token:
# 1. Go to https://account.mapbox.com/access-tokens/
# 2. Create a new token or copy an existing public token
# 3. Make sure it starts with "pk." not "sk."
# 4. Add it below (uncomment the line)
# NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_public_token_here
